# Commercial access is licensed per Store

Commercial access will be represented by Store Licenses, so each Store receives its own Plan and separately purchased Module access. This supports per-store pricing and lets Stores in one Organization use different commercial configurations rather than forcing an Organization-wide package.
