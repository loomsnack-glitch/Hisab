# Existing Stores receive a thirty-day Commercial Access Grant at enforcement launch

When Store Feature Entitlement enforcement first launches, every existing Store receives a Console-created complimentary Store Access Grant containing all currently available Modules for 30 days. This prevents an operational outage while preserving a bounded, visible migration path rather than a permanent unlicensed bypass.
