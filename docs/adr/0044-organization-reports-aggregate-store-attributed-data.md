# Organization Reports aggregate Store-attributed data

Reports are scope-aware read-only views, not a module whose records are owned by either an Organization or a Store. The Organization workspace shows aggregates across its Stores and can be filtered to one Store; a Store workspace shows only that Store's data. Aggregation never permits a cross-Store operational change.
