# POS Add-Ons Issue Drafts

These issue bodies were generated from [pos-add-ons-prd.md](C:/Users/smarty/Desktop/loomsnack/Hisab/docs/development/pos-add-ons-prd.md:1) after the vertical-slice breakdown was approved.

Intended publish order:

1. `Add-On catalog and Product attachment management`
2. `Sell a configured Product with trusted snapshots`
3. `Configuration-aware Draft Sale behavior`
4. `Lifecycle-safe configured billing`
5. `Add-On sales visibility and rollups`

Target label on publish: `ready-for-agent`

Publishing was not completed from this session because the repo is configured to use GitHub Issues via `gh`, but `gh` is not currently available in the environment.
